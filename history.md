# Photo Gateway — реализованная серверная часть

## Что сделано
- Разработан PHP-шлюз для пересылки фото между телефонами через интернет
- Шлюз работает как **очередь**: отправитель загружает фото → получатель забирает

## Эндпоинты API
| Действие | Запрос |
|----------|--------|
| Загрузить фото | `POST ?action=upload` (multipart, поле `photo`) |
| Список фото | `GET ?action=list` → JSON |
| Скачать фото | `GET ?action=get&id=xxx` → JPEG |
| Удалить фото | `GET ?action=delete&id=xxx` |
| Проверка связи | `GET ?action=ping` → `{"status":"ok"}` |

## Адрес
`http://attplus.in/album_gateway/index.php`

## Тестирование
- ✅ `ping` возвращает JSON
- ✅ Загрузка через curl работает
- ✅ Браузерная форма работает (после исправления `action="?action=upload"`)

```bash
`➜  album_gateway curl -A "Mozilla/5.0" http://attplus.in/album_gateway/index.php\?action\=ping
{"status":"ok","timestamp":1779031921}%                                                        ➜  album_gateway curl -X POST \
  -F "photo=@photo.jpg" \ 
  -A "Mozilla/5.0" \
  http://attplus.in/album_gateway/index.php\?action\=upload
{"status":"ok","id":"6a09e0a9b8e6d_1779032233.jpg"}
````



## Следующий шаг
Реализовать Kotlin-приложение для Android с двумя режимами:
- **Отправитель** — выбирает фото, отправляет на шлюз
- **Получатель (старый телефон)** — периодически проверяет `list`, скачивает новые фото и сохраняет в Google Фото с очисткой очереди

Сервер полностью готов.

---

## Прогресс на сегодня (2026-05-18)

Проект:

✅ Создан проект AlbumRelay с minSdk=26, targetSdk=34
✅ Настроены версии: Gradle 8.6, AGP 8.4.2, Kotlin 1.9.0
✅ Подключен Compose (BOM 2024.02.00, compiler 1.5.2)
✅ Основной экран на Compose: статус, лог, кнопки

app/src/main/java/top/smartable/albums/
├── MainActivity.kt          # Точка входа, экран на Compose
├── ui/                      # (будет)
├── data/                    # (будет)
└── service/                 # (будет)


✅ Репозиторий на GitHub создан
✅ .gitignore настроен (.idea/, build/, *.iml, local.properties)
✅ Первый коммит с рабочим проектом

### Ближайшие задачи:

- Вынести строки в strings.xml
- Реализовать PhotoGatewayApi.ping() для проверки соединения
- Подключить реальный статус вместо заглушки

---
### Дальний бэк-лог:

- доработать защиту PHP-шлюза (чтобы можно было настроить его отключение при странном поведении, удалить мусорную очередь и т.п.)
- Разделение UI и логики (MVVM) — ViewModel + StateFlow
- Фоновая служба-получатель (RelayService) с WorkManager
- Отправка фото через API из приложения-отправителя
- Сохранение фото в Google Фото (MediaStore API)
- Настройки: URL сервера, интервал синхронизации
- Обработка ошибок сети и повторные попытки
- Уведомления о статусе синхронизации


## Прогресс на 2026-05-20

**Сделано:**

- ✅ Проект собран, Compose UI работает
- ✅ `strings.xml` — строки вынесены
- ✅ `PhotoGatewayApi.ping()` — соединение проверяется, статус отображается
- ✅ `PhotoBridge.syncFromGateway()` — скачивает фото с сервера, сохраняет в `Pictures/Pixel3archive/`, удаляет из очереди
- ✅ Кнопка «Получить фото» запускает синхронизацию, логи пишутся в UI

**Встреченные ошибки и решения:**

- `NetworkOnMainThreadException` — обернули сетевые вызовы в `withContext(Dispatchers.IO)`
- `CLEARTEXT communication not permitted` — добавили `android:usesCleartextTraffic="true"` в манифест
- `403 Forbidden` — подменили User-Agent на браузерный
- `404 при скачивании файлов с пробелами` — проблема не критична, отложена

### Ближайшие задачи:

- Реализовать фоновую синхронизацию (WorkManager)
- Настройки: URL сервера, интервал синхронизации


### Дальний бэк-лог:

- Доработать защиту PHP-шлюза (отключение при странном поведении, очистка мусорной очереди)
- Отправка фото через API из приложения-отправителя
- URL-кодирование для имён с пробелами и спецсимволами
- Уведомления о статусе синхронизации
- Обработка ошибок сети и повторные попытки

---

## Прогресс на 2026-05-21

**Сделано:**

- ✅ Создан экран отправки фото (`SenderActivity`) с выбором нескольких фото из галереи
- ✅ Отображение списка выбранных фото с датой съёмки
- ✅ Реализована отправка фото на шлюз через `PhotoGatewayApi.uploadPhoto()`
- ✅ Отправка нескольких фото последовательно
- ✅ Кнопка «Отправить фото» на главном экране открывает `SenderActivity`
- ✅ Логи отправки выводятся в `Logcat` (переход на общее хранилище логов отложен)

**Встреченные ошибки и решения:**

- `ActivityNotFoundException` — забыли объявить `SenderActivity` в манифесте → добавили `<activity android:name=".SenderActivity" />`
- `GetMultipleContents()` возвращал 0 фото — заменили на `GetContent()` (одиночный выбор), мультивыбор работает через цикл
- Выбор фото только из Google Фото — проблема осталась, не решена (пользователь должен выбирать «Галерея» вручную)
- `sendPhotos` не видел `lifecycleScope` и `contentResolver` — метод перенесён внутрь класса, добавлены импорты
- Вызов метода класса из `@Composable` — передали `onSendPhotos` как колбэк

**Не решено:**

- ⚠️ Выборщик фото не показывает «Файлы» и "локальные фото" или медиа-источники (только репозиторий гугл-фото (облако))
- ⚠️ Нет общего хранилища логов (логи отправки не видны в MainActivity)

### Ближайшие задачи:

- Общее хранилище логов (`SharedPreferences` или `DataStore`)
- Настройки: URL сервера, интервал синхронизации
- Фоновая синхронизация (WorkManager)

### Дальний бэк-лог:

- Доработать защиту PHP-шлюза (отключение при странном поведении, очистка мусорной очереди)
- URL-кодирование для имён с пробелами и спецсимволами
- Уведомления о статусе синхронизации
- Обработка ошибок сети и повторные попытки

---

## Прогресс на 2026-05-22

**Сделано:**

- ✅ Создан экран настроек (`SettingsActivity`) с Compose UI
- ✅ Переключатель «Режим сервера» (включение/отключение функции получения фото)
- ✅ Поле ввода URL сервера с прокруткой длинного текста
- ✅ Кнопка проверки соединения с отображением результата в Toast и общем логе
- ✅ Диалог для изменения имени локальной папки моста (кликабельная карточка)
- ✅ Кнопки «Число файлов на гейте» и «Очистить гейт» (заглушки, требуют доработки)
- ✅ Кнопки «Число файлов на мосту» и «Очистить мост» (локальная папка)
- ✅ Кнопка «Очистить журнал» (общий лог событий)
- ✅ Общее хранилище логов через `SettingsManager` (SharedPreferences)
- ✅ Логи из `SenderActivity` теперь попадают в общий журнал
- ✅ Кнопка «Отправить фото» перенесена в главный экран, кнопка «Настройки» — в шестерёнку на TopAppBar

**Встреченные ошибки и решения:**

- `LocalContext.current` нельзя использовать в `PhotoGatewayApi` — передаём `Context` параметром
- `ping()` требовал `context` — добавили параметр во все вызовы
- Переменные `showDialog`, `bridgeFolderName`, `tempFolderName` не были объявлены — добавили `remember`
- Вызовы `getQueueCount` и `clearGateway` без корутины — обернули в `lifecycleScope.launch`
- Функция `clearBridge` отсутствовала — добавили с заглушкой для Android 10+
- Предупреждение о命名 функции `SettingsScreen` — подавили `@Suppress("FunctionName")`

**Не решено (отложено):**

- ⚠️ Выборщик фото в `SenderActivity` показывает только Google Фото, не показывает локальные файлы
- ⚠️ Функции `getQueueCount` и `clearGateway` в `PhotoGate` пока заглушки (возвращают 0 и true)
- ⚠️ Очистка моста на Android 10+ требует дополнительных разрешений (заглушка с Toast)

### Ближайшие задачи (завтра):

- MVVM-подстройка: вынести состояние `isServerMode` в `MainViewModel` с `StateFlow`
- Реализовать реальные методы `getQueueCount` и `clearGateway` в `PhotoGate`
- Доработать `clearBridge` для Android 10+ через `MediaStore`
- Фоновая синхронизация (WorkManager)

### Дальний бэк-лог:

- Доработать защиту PHP-шлюза
- URL-кодирование для имён с пробелами
- Уведомления о статусе синхронизации
- Обработка ошибок сети и повторные попытки
---
---
#2026-05-22
## Статистический анализ проекта Album Courier

### Легенда
- **1 SP** ≈ 30-60 минут реальной работы
- Фактическое время — экспертная оценка на основе истории чата

---

### Ключевые модули / задачи

| № | Модуль / Задача | Стори-поинты (оценка) | Факт. время (часы) | Расхождение |
|---|----------------|----------------------|--------------------|-------------|
| 1 | **Серверная часть (PHP-шлюз)** | 3 SP | 2 ч | ✅ быстрее |
| 2 | **Настройка проекта (Gradle, AGP, зависимости)** | 5 SP | 4 ч | ✅ быстрее |
| 3 | **MainActivity + Compose UI (статус, лог, кнопки)** | 3 SP | 2.5 ч | ✅ быстрее |
| 4 | **PhotoGatewayApi.ping() + MVVM (ViewModel)** | 3 SP | 3 ч | ✅ по плану |
| 5 | **PhotoBridge / PhotoGate (скачивание + сохранение)** | 5 SP | 4 ч | ✅ быстрее |
| 6 | **SenderActivity (выбор фото, список, отправка)** | 5 SP | 5 ч | ✅ по плану |
| 7 | **SettingsActivity (настройки, переключатели, диалог)** | 4 SP | 4 ч | ✅ по плану |
| 8 | **SharedPreferences (логи + настройки)** | 2 SP | 2 ч | ✅ по плану |
| 9 | **Интеграция Share Intent (приём фото из галереи)** | 2 SP | 1.5 ч | ✅ быстрее |
| 10 | **Отладка сетевых ошибок (CLEARTEXT, 403, Threading)** | 3 SP | 3.5 ч | ⚠️ чуть дольше |
| 11 | **Работа с MediaStore + EXIF (дата съёмки)** | 2 SP | 1.5 ч | ✅ быстрее |
| 12 | **GitHub + .gitignore + коммиты** | 1 SP | 1 ч | ✅ по плану |

---

### Итоги

| Метрика | Значение |
|---------|----------|
| **Всего SP (оценка)** | 38 SP |
| **Всего факт. времени** | 34 часа |
| **Среднее время на SP** | ~0.89 ч (53 мин) |
| **Точность планирования** | 89% (отлично) |

---

### Выполнено (100%)

- ✅ Серверная часть (PHP)
- ✅ Android-проект с Compose
- ✅ Отправка фото
- ✅ Приём фото
- ✅ Настройки + общий лог
- ✅ Share Intent (базовый)

---

### Осталось (бэк-лог)

| Задача | Оценка SP | Приоритет |
|--------|-----------|-----------|
| Фоновая синхронизация (WorkManager) | 5 SP | Высокий |
| Реальные методы `getQueueCount` / `clearGateway` | 2 SP | Средний |
| Очистка моста для Android 10+ | 2 SP | Средний |
| URL-кодирование пробелов в именах | 1 SP | Низкий |
| Защита PHP-шлюза (токен) | 2 SP | Низкий |
| Уведомления о статусе | 2 SP | Низкий |

**Осталось:** ~14 SP (≈12-14 часов)

---

#2026-05-30
https://github.com/CiscoCXSecurity/sslxray/blob/master/tlslite/utils/aesgcm.py
-- вариант библиотеки криптографической библиотеки без Rust

#2026-06-07
sudo airmon-ng start wlp3s0

Found 4 processes that could cause trouble.
Kill them using 'airmon-ng check kill' before putting
the card in monitor mode, they will interfere by changing channels
and sometimes putting the interface back in managed mode

    PID Name
    811 avahi-daemon
    856 avahi-daemon
    906 NetworkManager
    915 wpa_supplicant

PHY     Interface       Driver          Chipset

phy0    wlp3s0          ath10k_pci      Qualcomm Atheros QCA9377 802.11ac Wireless Network Adapter (rev 31)
                (mac80211 monitor mode vif enabled for [phy0]wlp3s0 on [phy0]wlp3s0mon)
                (mac80211 station mode vif disabled for [phy0]wlp3s0)

